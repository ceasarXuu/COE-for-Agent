{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.coe.local/domain/v1/gap.schema.json",
  "allOf": [
    { "$ref": "../../common/base-node.schema.json" },
    {
      "type": "object",
      "required": ["kind", "question", "status", "priority"],
      "properties": {
        "kind": { "const": "investigation.gap" },
        "question": { "type": "string", "minLength": 1, "maxLength": 2000 },
        "status": { "type": "string", "enum": ["open", "in_progress", "blocked", "resolved", "waived"] },
        "priority": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
        "blockedRefs": { "type": "array", "items": { "type": "string" }, "uniqueItems": true }
      }
    }
  ],
  "unevaluatedProperties": false
}