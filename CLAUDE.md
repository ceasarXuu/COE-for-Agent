# COE for Agent

Follow `AGENTS.md` as the project source of truth.

When the `coe-investigation` MCP server is connected:

- use MCP tools for all case mutations
- use MCP resources for all case reads
- avoid proposing repairs before the evidence chain is recorded
- escalate reviewer-only actions instead of simulating them
